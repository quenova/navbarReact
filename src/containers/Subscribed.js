import React from 'react';
import { Container, Typography } from '@mui/material';

const Subscribed = () => (
  <Container>
    <Typography variant="h4" gutterBottom>
      Subscribed
    </Typography>
    <Typography variant="body1">
      This is the subscribed page.
    </Typography>
  </Container>
);

export default Subscribed;
